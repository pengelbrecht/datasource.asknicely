# Documented here https://asknicely.asknice.ly/help/apidocs/responses
make_responses_url <- function(
  domain_key,
  sort_direction = "asc"
  pagesize = 50000,
  pagenumber = 1
  since_time,
  end_time) {

  glue::glue("")
}
